package src;

public class Article extends Publication {
    Article(String title, String[] authors, int publishingYear, int numOfCitations) {
        super(title, authors, publishingYear, numOfCitations);
    }
}
