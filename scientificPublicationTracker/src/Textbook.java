public class Textbook extends Book{
    Textbook(String title, String[] authors, int publishingYear, int numOfCitations, String publishingCompany, int numOfPages) {
        super(title, authors, publishingYear, numOfCitations, publishingCompany, numOfPages);
    }
}
